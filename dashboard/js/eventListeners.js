// eventListeners.js
import { approveUser, revokeUser } from './adminFunctions.js';

export function setupEventListeners(nftContract, affiliateContract) {
    document.getElementById('approveUserBtn').addEventListener('click', async () => {
        // Implementation...
    });

    // Continue with other event listeners setup
}

